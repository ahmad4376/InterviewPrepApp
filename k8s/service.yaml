apiVersion: v1
kind: Service
metadata:
  name: interview-prep-service
spec:
  selector:
    app: interview-prep-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
